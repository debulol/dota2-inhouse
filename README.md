# 大象公会 Dota2 内战系统

一个为大象公会成员设计的 Dota2 内战 Roll 点选人、战绩记录网站。

## 功能特性

- 🎲 **Roll 点选队长**：随机 1-100 点数，前两名自动成为队长
- 👥 **队伍选人**：队长依次选择队员组建 5v5 队伍
- 📊 **战绩统计**：记录每位玩家的胜场、负场和胜率
- 🤝 **组合胜率**：统计玩家之间的配合胜率
- 🏠 **房间系统**：支持创建/加入房间，24小时自动过期

## 技术栈

### 前端
- **框架**: React 18 + Vite
- **路由**: React Router v6
- **状态管理**: Zustand
- **UI组件**: Tailwind CSS + shadcn/ui
- **HTTP客户端**: Axios

### 后端
- **数据库**: Supabase (PostgreSQL)
- **实时通信**: Supabase Realtime (WebSocket)
- **部署**: Vercel

## 项目结构

```
dota2-inhouse/
├── src/
│   ├── components/      # 可复用组件
│   ├── pages/          # 页面组件
│   ├── store/          # Zustand状态管理
│   ├── lib/            # 工具库和配置
│   ├── hooks/          # 自定义Hooks
│   └── data/           # 静态数据
├── public/
│   └── heroes/         # 英雄头像资源
└── supabase/
    └── migrations/     # 数据库迁移文件
```

## 快速开始

### 1. 克隆项目

```bash
git clone <repository-url>
cd dota2-inhouse
```

### 2. 安装依赖

```bash
npm install
```

### 3. 配置环境变量

复制 `.env.example` 到 `.env`：

```bash
cp .env.example .env
```

填入你的 Supabase 配置：

```env
VITE_SUPABASE_URL=your_supabase_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 4. 初始化数据库

在 Supabase 控制台执行 `supabase/migrations/001_initial_schema.sql`

### 5. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173`

## 部署

### Vercel 部署

1. 连接 GitHub 仓库到 Vercel
2. 配置环境变量（Supabase URL 和 Key）
3. 点击部署

### Supabase 配置

1. 在 Supabase 创建新项目
2. 执行数据库迁移脚本
3. 配置 Row Level Security（可选）
4. 获取 API URL 和 anon key

## 开发指南

### 添加新玩家

直接在 Supabase 控制台的 `players` 表中插入数据，或修改初始化脚本。

### 修改战绩

在 Supabase 控制台直接修改 `players` 表的 `total_wins` 和 `total_losses` 字段。

### 自定义房间逻辑

主要逻辑在 `src/pages/RoomLobby.jsx` 和数据库函数中。

## License

MIT
